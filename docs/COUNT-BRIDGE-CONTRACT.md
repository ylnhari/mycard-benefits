# Count bridge boundary

No count bridge is enabled or supported. Card counts remain local to this
application and are never sent to another application or service. There is no
counterpart opt-in or external review path.

This is a local boundary, not synchronization. There is no scheduler, implicit
aggregate, or automatic transmission.
