# Continue MyCard Benefits here

This is the stable starting point for the next coding session, especially when
Google Antigravity will work independently.

1. Read `AGENTS.md` completely.
2. Read `coordination/ORCHESTRATION-HANDOFF-2026-08-07.md` completely. It is the
   authoritative checkpoint, review disposition, private-artifact inventory,
   recovery guide, and ordered continuation plan.
3. Work from the current `main` commit after verifying `git status --short`,
   `git log -5 --oneline`, and the handoff's recorded checkpoint.
4. Do not repeat the questionnaire, rescan Drive/Family Finance/the workbook,
   or start external agents merely to continue. Antigravity may implement and
   test the remaining work by itself.
5. Treat the Claude and MC-085 branches as unapproved references. Do not merge
   them unchanged; their remaining review findings are listed in the handoff.
6. Never commit or place private card values in prompts. The already captured
   private inventory remains encrypted under ignored local paths and must be
   consumed only through supported local APIs.
7. Do not push merely because an implementation is complete. Re-run the
   repository gates and follow the exact publication gate in `AGENTS.md`.

If another runner is available, it is optional. The next agent remains fully
capable of completing the plan alone from these files.
